{% extends 'base.html.twig' %}

{% block title %}PlanningEntry{% endblock %}

{% block body %}
    <h1>PlanningEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planning_entry.id }}</td>
            </tr>
            <tr>
                <th>Is_absent</th>
                <td>{{ planning_entry.isAbsent ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ planning_entry.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planning_entry_index') }}">back to list</a>

    <a href="{{ path('app_planning_entry_edit', {'id': planning_entry.id}) }}">edit</a>

    {{ include('planning_entry/_delete_form.html.twig') }}
{% endblock %}

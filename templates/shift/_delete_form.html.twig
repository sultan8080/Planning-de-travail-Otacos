<form method="post" action="{{ path('app_shift_delete', {'id': shift.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shift.id) }}">
    <button class="btn">Delete</button>
</form>

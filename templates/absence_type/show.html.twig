{% extends 'base.html.twig' %}

{% block title %}AbsenceType{% endblock %}

{% block body %}
    <h1>AbsenceType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ absence_type.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ absence_type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_absence_type_index') }}">back to list</a>

    <a href="{{ path('app_absence_type_edit', {'id': absence_type.id}) }}">edit</a>

    {{ include('absence_type/_delete_form.html.twig') }}
{% endblock %}
